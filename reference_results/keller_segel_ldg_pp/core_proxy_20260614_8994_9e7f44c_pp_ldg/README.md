# Core-local proxy diagnostics (§5.5)

Post-processing only. Regenerate with `experiments/keller_segel/core_local_proxy/analyze_core_proxy.py` then `plot_core_proxy.py`. Honest finding: reconstruction-free radii collapse robustly; candidate T* is window-sensitive (1.4x-4.5x), so no continuum blow-up time is quoted; core-local proxy == global proxy for the grid baseline. See `manifest.json`.
